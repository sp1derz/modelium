{
  "dashboard": {
    "title": "Modelium - System Overview",
    "tags": ["modelium", "overview"],
    "timezone": "browser",
    "panels": [
      {
        "id": 1,
        "title": "Model Ingestion Rate",
        "type": "graph",
        "targets": [
          {
            "expr": "rate(modelium_models_ingested_total[5m])",
            "legendFormat": "{{ namespace }}"
          }
        ],
        "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0}
      },
      {
        "id": 2,
        "title": "Conversion Success Rate",
        "type": "graph",
        "targets": [
          {
            "expr": "rate(modelium_conversions_success_total[5m]) / rate(modelium_conversions_total[5m])",
            "legendFormat": "Success Rate"
          }
        ],
        "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0}
      },
      {
        "id": 3,
        "title": "Active Conversions",
        "type": "stat",
        "targets": [
          {
            "expr": "modelium_active_conversions",
            "legendFormat": "Active"
          }
        ],
        "gridPos": {"h": 4, "w": 6, "x": 0, "y": 8}
      },
      {
        "id": 4,
        "title": "Deployed Models",
        "type": "stat",
        "targets": [
          {
            "expr": "count(kube_pod_labels{label_serving_kserve_io_inferenceservice!=\"\"})",
            "legendFormat": "Deployed"
          }
        ],
        "gridPos": {"h": 4, "w": 6, "x": 6, "y": 8}
      },
      {
        "id": 5,
        "title": "Average Conversion Time",
        "type": "graph",
        "targets": [
          {
            "expr": "rate(modelium_conversion_duration_seconds_sum[5m]) / rate(modelium_conversion_duration_seconds_count[5m])",
            "legendFormat": "Avg Time (s)"
          }
        ],
        "gridPos": {"h": 8, "w": 12, "x": 12, "y": 8}
      },
      {
        "id": 6,
        "title": "vLLM/Ray Inference Throughput",
        "type": "graph",
        "targets": [
          {
            "expr": "rate(modelium_inference_requests_total[5m])",
            "legendFormat": "{{ model_name }} - {{ runtime }}"
          }
        ],
        "gridPos": {"h": 8, "w": 12, "x": 0, "y": 16}
      },
      {
        "id": 7,
        "title": "Inference Latency (P95)",
        "type": "graph",
        "targets": [
          {
            "expr": "histogram_quantile(0.95, rate(modelium_inference_duration_seconds_bucket[5m]))",
            "legendFormat": "{{ model_name }} - {{ runtime }}"
          }
        ],
        "gridPos": {"h": 8, "w": 12, "x": 12, "y": 16}
      },
      {
        "id": 8,
        "title": "GPU Utilization",
        "type": "graph",
        "targets": [
          {
            "expr": "DCGM_FI_DEV_GPU_UTIL",
            "legendFormat": "{{ gpu }} - {{ node }}"
          }
        ],
        "gridPos": {"h": 8, "w": 12, "x": 0, "y": 24}
      },
      {
        "id": 9,
        "title": "GPU Memory Usage",
        "type": "graph",
        "targets": [
          {
            "expr": "DCGM_FI_DEV_FB_USED / DCGM_FI_DEV_FB_FREE * 100",
            "legendFormat": "{{ gpu }} - {{ node }}"
          }
        ],
        "gridPos": {"h": 8, "w": 12, "x": 12, "y": 24}
      },
      {
        "id": 10,
        "title": "Models Loaded per GPU",
        "type": "stat",
        "targets": [
          {
            "expr": "modelium_models_loaded_per_gpu",
            "legendFormat": "GPU {{ gpu_id }}"
          }
        ],
        "gridPos": {"h": 4, "w": 6, "x": 0, "y": 32}
      },
      {
        "id": 11,
        "title": "Orchestrator Decisions",
        "type": "graph",
        "targets": [
          {
            "expr": "rate(modelium_orchestrator_actions_total[5m])",
            "legendFormat": "{{ action }}"
          }
        ],
        "gridPos": {"h": 8, "w": 12, "x": 6, "y": 32}
      }
    ],
    "refresh": "30s",
    "schemaVersion": 27,
    "style": "dark",
    "time": {
      "from": "now-6h",
      "to": "now"
    }
  }
}

